import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById('root'));

const img = (
  <img src="logo512.png" alt="React logo"></img>
);

root.render(img);
